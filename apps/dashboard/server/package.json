{
  "name": "@aios/monitor-server",
  "version": "1.0.0",
  "description": "Real-time event server for monitoring Claude Code activity",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
