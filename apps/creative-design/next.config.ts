import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Indicador de dev do Next.js desativado — não pode vazar nas capturas
  // dos criativos renderizados.
  devIndicators: false,
};

export default nextConfig;
